# 实时效率 PK 接口文档（转交服务端开发者）

PC 挂机工具主卡最左新增「实时效率 PK」区：客户端每 ~10 秒上报一次**当前预估 EXP/h**，
服务端算出名次返回，主卡显示 `第x名`。本文是**给服务端开发者的对接说明**——客户端侧
已按本文实现完毕（Rust：`net.rs::post_pk` + `pk.rs` 上报线程 + `app.rs` 渲染）。

与 exp-api.md **复用同一套 v2 鉴权**：token 是 `POST /api/v2/exp/token` 换来的 JWT，
`sub` 即主板 MD5 uid（设备身份）。鉴权走通这个接口就行，不需要新的密钥机制。

---

## 1. 服务端需要做的事（Checklist）

1. **新增路由** `POST /api/v2/exp/pk`，复用 v2 的 JWT 校验中间件：
   - 无 `Authorization: Bearer <token>` / token 失效 → `401 {"ok":false,"error":"令牌无效或已过期"}`。
   - **以 JWT 的 `sub` 作为设备身份（榜单 key）**，不要信请求体里的任何设备字段。
2. **请求体验证**（命中即 `400 {"ok":false,"error":"<原因>"}`）：
   - 请求体不是合法 JSON；`exp_per_hour` 缺省/非数字/<0/>1e9；`ts` 缺省/非整数/过大超前服务器。
3. **内存榜单（核心约束，来自产品要求）**：
   - 只维护**一个数组**，元素 ≤ **999** 个；放不下时**丢弃最旧的**。
   - 数组按时间戳**逆序**（最新在前 → 最旧在尾 → 裁剪时从尾部丢）。
   - **强烈建议按设备去重（upsert）**：同一 `sub` 的新上报**覆盖**其旧元素（更新 `ts`/`exp_per_hour`），
     使每台设备只占 1 个槽位。若只增不去重，一台设备 10s 一条，约 2.8 小时就会把 999 槽
     全占满、把其他人全部挤出——榜单就废了。
   - 时间戳口径二选一（请与服务端一致并在代码注释里写清）：
     a. 以**请求体 `ts`** 排序（客户端已开系统时间同步，可靠）；建议防御性校验
        `ts` 不超过服务器当前时间 +5 分钟，超了按服务器时间处理或拒绝。
     b. 以**服务器收到时刻**排序（无视 `ts`，最简单）。客户端 `ts` 仅当诊断日志用。
   - 持久化**非必需**：这是"实时"榜，重启清空可以接受。若要保留，落到文件/内存库即可，
     不是本项目重点。
4. **名次口径（决定"第x名"，请拍板一种）**：
   - **推荐（效率 PK 的本意）**：在榜单数组内按 `exp_per_hour` **降序**排，
     `rank = 1 + 比你 exp_per_hour 高的设备数`；同分按 `ts` 更早者靠前（先到先得）。
   - 备选（严格按"逆时间序"字面）：`rank = 1 + ts 比你新的设备数`——但这样名次 =
     "谁最后上报"，跟效率无关，不推荐。
   - 客户端只显示你返回的数字，口径由你定，**无需改客户端**。
   - **返回的 `rank` 可以 >999**（比如你另有一个不裁剪的全量榜想用它）。客户端显示规则：
     `rank ≤ 999` → `第N名`；`rank > 999` → `第999+名`（已在客户端实现）。
5. **限频**：服务端实际**只按 `sub`（设备）限频**（同 `sub` 两次成功 <5s → `429`），
   **不做公网 IP 限频**——共享 IP/NAT 下会多台工具同 IP 同时 ~10s 心跳，按 IP 会互相 429 空转；
   PK 不是防刷主战场，`sub` 已被 JWT 限定为持密钥的真客户端。客户端节拍是 10s，正常不会撞；
   撞了客户端会跳过本轮等下一轮。
6. **日志**：参照 `[exp]` 上报的打印，加 `[pk] 拒绝：<原因> | 请求体=…`，方便联调排查。

---

## 2. 接口定义

| 项目 | 值 |
|---|---|
| 方法 / 地址 | `POST /api/v2/exp/pk` |
| 请求头 | `Authorization: Bearer <token>`、`Content-Type: application/json` |
| 请求体上限 | 64 KB（正常 ~100 B） |
| 鉴权 | 与 `POST /api/v2/exp/report` 相同的 v2 JWT；`sub` = 设备 ID |

### 2.1 请求体示例

```json
{
  "board_id": "004abf2e...32位md5uid",
  "ts": 1788481788,
  "exp_per_hour": 360000
}
```

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| `board_id` | string | 1~64（字母/数字/`_`/`-`） | 主板 MD5 uid（与 JWT `sub` 同值）。**仅供参考/日志**，身份一律以 `sub` 为准，勿信此处 |
| `ts` | integer | Unix 秒（UTC） | 客户端发送时刻。排序口径见 §1.3 |
| `exp_per_hour` | integer | 0 ~ 1,000,000,000 | 客户端算好的**预估 EXP/时**（与 v2 上报同源），**0 合法** |

### 2.2 响应

成功 `200`：

```json
{ "ok": true, "rank": 3, "total": 57 }
```

| 字段 | 类型 | 说明 |
|---|---|---|
| `rank` ★ | integer ≥1 | 本次上报的名次（口径见 §1.4）。**客户端只认这一个字段** |
| `total` ✎ | integer | 榜单当前设备数（可选；客户端暂存供诊断/未来展示） |

失败统一 `{"ok":false,"error":"<原因>"}`：

| 状态码 | error 示例 | 客户端行为 |
|---|---|---|
| 400 | 字段校验文案 | 记日志，等下轮（客户端数据一般合法，不应常出现） |
| 401 | `令牌无效或已过期` | 客户端会**换 token 重试一次**（与 v2 上报一致） |
| 429 | `上报过于频繁` | 记日志，等下轮（10s 节拍正常不触发） |
| 500 | — | 记日志，等下轮 |

---

## 3. 客户端行为（方便对齐，不需要服务端处理）

- **上报节拍**：每 10s 一次。**仅当"正在刷怪且有实时样本"**时上报——即最近 ~30s 内成功
  读到过 EXP 且已算出 预估EXP/时。切走/最小化/关游戏造成的暂停会使读数变陈旧 → **不再
  上报**，本设备凭 §1.3 的裁剪自然从榜上淡出。
- 连续 ~50s（约 5 轮）没能成功上报（离线或网络失败）→ 客户端把显示清成 `-`；
  恢复刷怪后 1~2 轮内重上榜。
- 断网/服务端没上线不影响主卡其它指标，只是 PK 区保持 `-` 或最近一次名次。

---

## 4. 联调自测

**先换 token（deviceId = 你的主板 md5 uid，也可随便编一个合法字符串联调）：**

```bash
# Linux / macOS（jq 取 token）
UID="test-board-001"
TOKEN=$(curl -s -X POST "https://你的域名/api/v2/exp/token" \
  -H "X-Exp-Device-Secret: zhuzhu" \
  -H "Content-Type: application/json" \
  -d "{\"deviceId\":\"$UID\"}" | jq -r .token)

# 上报一次拿名次
curl -s -X POST "https://你的域名/api/v2/exp/pk" \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d "{\"board_id\":\"$UID\",\"ts\":$(date +%s),\"exp_per_hour\":360000}"
# → {"ok":true,"rank":1,"total":1}
```

**本地服务端（127.0.0.1:3001）** 同理把域名换成 `http://127.0.0.1:3001`；PC 工具侧用
`MXD_BAR_API=local` 环境变量或 data.ini `[net] base=local` 切到本地后，`--nettest` 会
自动打一遍「换 token → 上报 → PK」（debug 构建）。

---

## 5. 常见坑速查

1. **`ts` 超前服务器太多**：客户端时钟比服务器快 5 分钟以上，建议拒或按服务器时间处理
   （与 v2 上报同款防御）。
2. **数组不去重**：见 §1.3——务必按 `sub` upsert，否则单机刷屏会挤掉所有人。
3. **同分/同 ts**：排序需有稳定 tie-break（先到先得或按 `sub` 字典序），否则名次在服务端
   重启或并发时会抖。
4. **`exp_per_hour` 巨大**：客户端上限 1e9，服务端再按 v1/v2 的口径兜底校验一次即可。
5. **身份**：排名/去重一律用 JWT `sub`；body 里重复发的 `board_id` 只是方便看日志。
